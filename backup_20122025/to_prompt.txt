You are a CLI coding agent working on this repo. Your task is to implement a new unified backend + simplify the VisionOS app’s debug UX, based on the working reference `basic_main_api_with_uxplay.py`.

GOALS
1) Backend: Create a single runnable API that combines:
   - The existing “main_api” capabilities (RealSense RGB+depth feeds, RS→AVP depth transform, RS-view ArUco pose detection, FoundationPose/object pose estimation endpoints, any existing pipeline endpoints like /rgb_frame, /intrinsics, /mask, /disparity, /avp_pose).
   - The UxPlay-based capture + streaming model from `basic_main_api_with_uxplay.py` (UxPlay spawn, stdout BGR capture, rate-limited processing thread, /rgb + /aruco + /mjpeg-like streaming).
   - Match the architecture of `basic_main_api_with_uxplay.py`: single-process Flask app, background threads for capture + processing, endpoints are fast and lock-protected.
   - Preserve (and don’t break) the “main_api” endpoints if they already exist, but add UxPlay equivalents as separate endpoints or merged endpoints where sensible.

2) VisionOS app: Remove all features that attempt accurate 3D overlay placement in immersive space of:
   - RS pose (RealSense)
   - ArUco pose
   - Object/FoundationPose pose
   Because accurate world-aligned 3D overlay isn’t possible without camera access.
   Keep the ability to fetch and display debug imagery and numeric pose values.

3) Logging: Improve debug visibility:
   - Make the VisionOS app show clearer logs in its debugger/log view (timestamped lines, last N entries, distinct tags: [RS], [ARUCO], [OBJ], [NET], [ERROR]).
   - Ensure logs include endpoint status, pose availability, and key numeric pose fields (tvec / translation meters, quaternion).

4) Web debug viewer: Add a browser-based debug UI:
   - Similar to the current `/mjpeg` endpoint idea, but expanded.
   - Provide:
     a) a live image view (MJPEG or periodic JPEG fetch) for relevant streams (UxPlay view, RS view, and any “annotated” view you already have),
     b) a “2D Pose Overlay” view that draws the 3 poses (RS, ArUco, Obj) on top of an image in 2D with clear labels and distinct styling (label text must explicitly say “RS”, “Aruco”, “Obj”).
   - The overlay must be 2D only (projected into the image). Do NOT attempt 3D world overlay.
   - Add an HTML page endpoint, e.g. `/debug`, that serves the viewer. Keep it dependency-light (plain HTML/JS/CSS is fine).
   - Include a JSON endpoint that returns the latest poses in a single payload for the viewer, e.g. `/poses` returning:
     { timestamp, rs_pose: {...}, aruco_pose: {...}, obj_pose: {...}, status: {...} }
   - Include clear labels and last-update timestamps for each pose.

BACKEND IMPLEMENTATION DETAILS
- Create a new entrypoint script (or refactor) named something like:
  `unified_main_api_with_uxplay.py`
  It should:
  - Start UxPlay capture and processing threads like the reference.
  - Start/initialize the RealSense pipeline (from main_api) if available/configured.
  - Run ArUco detection on:
    - the UxPlay stream (as in reference)
    - AND optionally on the RealSense RGB stream (feature parity with main_api).
  - Keep pose results in a shared thread-safe store with locks.
  - Expose endpoints:
    - `/health` (must include rs status, uxplay status, frame counts)
    - `/rgb` (UxPlay RGB JPEG)
    - `/aruco` (UxPlay annotated + aruco pose)
    - `/rs/rgb`, `/rs/depth` (or whatever naming makes sense; preserve existing main_api routes if present)
    - `/rs/aruco` (annotated RS view + pose)
    - `/poses` (combined latest RS/Aruco/Obj poses)
    - `/mjpeg` (UxPlay annotated stream)
    - `/rs/mjpeg` (optional)
    - `/debug` (HTML viewer)
  - Ensure CORS is enabled for the viewer.
  - Ensure timeouts and polling intervals are configurable via CLI flags.
  - Keep the original `basic_main_api_with_uxplay.py` working; do not delete it.

VISIONOS APP CHANGES
- Remove/disable any RealityKit/ImmersiveSpace 3D placement for RS/Aruco/Obj axes/entities.
- Keep the networking models that fetch poses and images, but the UI should focus on:
  - showing the latest annotated image(s),
  - showing textual pose values,
  - showing improved logs.
- Add a prominent note in the debug UI: “3D overlay disabled (camera access unavailable)” (exact wording can vary, but make it explicit).

2D OVERLAY REQUIREMENTS (WEB VIEWER)
- The 2D overlay should be drawn on a canvas over the image.
- Each pose must be clearly labeled near its projected origin:
  - “RS”
  - “Aruco”
  - “Obj”
- If a pose is missing, show “RS: n/a” etc.
- Use the camera intrinsics that correspond to the image source for projection (e.g., RS K for RS view, UxPlay K for UxPlay view, etc.). If intrinsics are unavailable, degrade gracefully (no overlay, label n/a).
- Keep units consistent (meters).

DELIVERABLES
- A new unified backend script + any supporting modules/classes.
- Updated VisionOS app code removing 3D overlay and improving logging.
- New `/debug` web viewer page + endpoints needed to support it.
- Update README with:
  - how to run unified API,
  - endpoint list,
  - how to open the web debug viewer,
  - what was removed in VisionOS app and why.

CONSTRAINTS
- Do not attempt true world-aligned 3D overlay; only 2D projection overlays.
- Keep code clean and modular: separate capture, processing, pose stores, and Flask endpoints.
- Prefer minimal dependencies; use built-in Flask/static serving for the web viewer.

Start by:
1) Scanning the repo for the existing “main_api” implementation and listing key features/endpoints it already provides.
2) Designing the unified architecture (classes + shared pose store).
3) Implementing unified backend first (including `/debug` viewer), then the VisionOS app simplification.

