#### make the python client ask the api for the available models, and for the pose request gives the name of the model
#### make the api give the model names, and uses the model name in the pose request to forward the right model to the pose api
#### monitor api communication latency